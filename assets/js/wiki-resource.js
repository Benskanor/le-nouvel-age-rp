(() => {
  const script=document.currentScript,root=new URL('../../',script.src),dataUrl=new URL('data/wiki.json',root),slug=document.body.dataset.resourceSlug;
  const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
  const show=v=>(v===undefined||v===null||v===''||v==='—')?'<span class="undocumented">Non applicable / non documenté</span>':esc(v);
  const imgUrl=p=>p?new URL(p,root).href:'';
  fetch(dataUrl).then(r=>r.json()).then(db=>{
    const r=db.resources.find(x=>x.slug===slug);if(!r)throw new Error('Ressource inconnue');
    document.title=`${r.name} | Wiki Le Nouvel Âge`;document.querySelector('#resourceTitle').textContent=r.name;document.querySelector('#resourceCategoryLabel').textContent=`${r.category} · ${r.realm} · ${r.version}`;document.querySelector('#resourceLead').textContent=r.description;document.querySelector('#obtentionText').textContent=r.obtention||'—';document.querySelector('#usesList').innerHTML=(r.uses?.length?r.uses:['À documenter']).map(x=>`<li>${esc(x)}</li>`).join('');
    document.querySelector('#resourceInfobox').innerHTML=`<div class="infobox-title">${esc(r.name)}</div><div class="infobox-icon ${r.image?'has-image':''}">${r.image?`<img src="${imgUrl(r.image)}" alt="${esc(r.name)}">`:`<span>${esc(r.icon||'LNA')}</span>`}</div><dl><div><dt>Catégorie</dt><dd>${esc(r.category)}</dd></div><div><dt>Origine</dt><dd>${esc(r.realm)}</dd></div><div><dt>Version</dt><dd>${esc(r.version)}</dd></div><div><dt>État</dt><dd>${esc(r.status)}</dd></div><div><dt>Station</dt><dd>${show(r.station)}</dd></div><div><dt>Rendement</dt><dd>${show(r.yield)}</dd></div><div><dt>Stack</dt><dd>${show(r.stack)}</dd></div><div><dt>Poids</dt><dd>${show(r.weight)}</dd></div><div><dt>Péremption</dt><dd>${show(r.spoil)}</dd></div></dl>`;
    document.querySelector('#recipeText').innerHTML=show(r.recipe);document.querySelector('#craftTime').innerHTML=r.craftTime?`<strong>Temps de craft :</strong> ${esc(r.craftTime)}`:'';
    const tech=r.technical||{};document.querySelector('#technicalGrid').innerHTML=`<div><span>Blueprint / classe</span><code>${show(tech.blueprint)}</code></div><div><span>Commande GFI</span><code>${show(tech.gfi)}</code></div><div><span>Pondération pêche</span><code>${show(r.lootWeight)}</code></div>`;
    const related=db.resources.filter(x=>x.slug!==r.slug&&(x.category===r.category||x.realm===r.realm)).slice(0,6);document.querySelector('#relatedResources').innerHTML=related.map(x=>`<a href="${new URL(`wiki/ressources/${x.slug}/`,root)}">${x.image?`<img src="${imgUrl(x.image)}" alt="">`:`<span>${esc(x.icon||'LNA')}</span>`}<div><small>${esc(x.category)}</small><strong>${esc(x.name)}</strong></div></a>`).join('');
  }).catch(e=>{console.error(e);document.querySelector('#resourceLead').textContent='Cette fiche n’a pas pu être chargée.'});
})();
